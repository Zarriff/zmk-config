/*
 * Copyright (c) 2020 The ZMK Contributors
 *
 * SPDX-License-Identifier: MIT
 */

#include <behaviors.dtsi>
#include <dt-bindings/zmk/bt.h>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/rgb.h>
#include "zmk-helpers/key-labels/42.h"                                                  // Source key-labels.

#define KEYS_L LT0 LT1 LT2 LT3 LT4 LT5 LM0 LM1 LM2 LM3 LM4 LM5 LB0 LB1 LB2 LB3 LB4 LB5  // Left-hand keys.
#define KEYS_R RT0 RT1 RT2 RT3 RT4 RT5 RM0 RM1 RM2 RM3 RM4 RM5 RB0 RB1 RB2 RB3 RB4 RB5  // Right-hand keys.
#define THUMBS LH2 LH1 LH0 RH0 RH1 RH2                                                  // Thumb keys.

&mt {
    flavor = "balanced";
    tapping-term-ms = <280>;
    quick-tap-ms = <175>;
    require-prior-idle-ms = <125>;
    bindings = <&kp>, <&kp>;

    hold-trigger-key-positions = <KEYS_R THUMBS>;
    hold-trigger-on-release;
};

&mt {
    flavor = "balanced";
    tapping-term-ms = <280>;
    quick-tap-ms = <175>;
    require-prior-idle-ms = <125>;
    bindings = <&kp>, <&kp>;

    hold-trigger-key-positions = <KEYS_L THUMBS>;
    hold-trigger-on-release;
};

/ {
    chosen { zmk,physical-layout = &default_layout; };
};

/ {
    behaviors {
        lt_repeat: layer_tap_repeat {
            compatible = "zmk,behavior-hold-tap";
            label = "LAYER_TAP_REPEAT";
            #binding-cells = <2>;
            tapping-term-ms = <200>;

            // Set this to enable tap-then-hold-to-repeat

            quick-tap-ms = <200>;
            flavor = "tap-preferred";
            bindings = <&mo>, <&kp>;
        };

        skq: sticky_key_quick_release {
            compatible = "zmk,behavior-sticky-key";
            #binding-cells = <1>;
            bindings = <&kp>;
            release-after-ms = <2000>;
            quick-release;
            ignore-modifiers;
        };

        sl_mo: sticky_layer_momentary_layer {
            compatible = "zmk,behavior-hold-tap";
            #binding-cells = <2>;
            flavor = "hold-preferred";
            tapping-term-ms = <200>;
            bindings = <&mo>, <&sl>;
        };

        tog_on: toggle_layer_on_only {
            compatible = "zmk,behavior-toggle-layer";
            #binding-cells = <1>;
            display-name = "Toggle Layer On";
            toggle-mode = "on";
            // locking;
        };
    };

    combos {
        compatible = "zmk,combos";

        escape {
            bindings = <&kp ESCAPE>;
            key-positions = <3 2>;
        };

        tab {
            bindings = <&kp TAB>;
            key-positions = <26 27>;
        };

        enter {
            bindings = <&kp ENTER>;
            key-positions = <32 33>;
        };
    };

    keymap {
        compatible = "zmk,keymap";

        default_layer {
            display-name = "BASE";

            // ------------------------------------------------------------------------------------
            // | App switch |  Q  |  W  |  F  |  P  |  B  ||  J  |  L   |  U  |  Y  |  '  | CAPWRD |
            // | -          |  A  |  R  |  S  |  T  |  G  ||  K  |  N   |  E  |  I  |  O  |  ;     |
            // | BSPC       |  Z  |  X  |  C  |  D  |  V  ||  H  |  H   |  ,  |  .  |  /  | LYR4   |
            //                    | Ray | SPC/LYR2 | LYR1 || LYR3 | SHIFT  | Leader |

            bindings = <
&kp LG(SPACE)  &kp Q  &kp W  &kp F  &kp P  &kp B  &kp J  &kp L  &kp U  &kp Y  &kp SINGLE_QUOTE  &caps_word  &kp MINUS  &mt LEFT_CONTROL A  &mt LEFT_ALT R  &mt LEFT_SHIFT S  &mt LEFT_GUI T  &kp G  &kp K  &mt LEFT_GUI N  &mt LEFT_SHIFT E  &mt LEFT_ALT I  &mt LEFT_CONTROL O  &kp SEMICOLON  &kp BACKSPACE  &kp Z  &kp X  &kp C  &kp D  &kp V  &kp M  &kp H  &kp COMMA  &kp DOT  &kp FSLH  &mo 4  &kp LA(SPACE)  &lt_repeat 2 SPACE  &mo 1  &sl_mo 3 3  &skq LEFT_SHIFT  &kp LS(LA(LG(LC(SPACE))))
            >;
        };

        lower_layer {
            display-name = "NAV";

            // ---------------------------------------------------------------------------------------------
            // |  |      |     |  CMD C |  CMD V |  ||        |      | PG_DN | PG_UP       |       | TAB   |
            // |  | CTRL | ALT | SHIFT  | CMD    |  || CLPBRD | LFT  | DWN   | UP          | RGT   | ENTER |
            // |  |      |     |        |        |  ||        | BSPC | DEL   | CMD SHIFT K | CMD / | ESC   |
            //                             |  |  |  ||  |  |  |

            bindings = <
&trans  &none  &none  &kp LG(C)  &kp LG(V)  &none  &none  &none  &kp PAGE_DOWN  &kp PAGE_UP  &none  &kp TAB  &none  &kp LEFT_CONTROL  &kp LEFT_ALT  &kp LEFT_SHIFT  &kp LEFT_GUI  &none  &kp LG(LS(V))  &kp LEFT_ARROW  &kp DOWN_ARROW  &kp UP_ARROW  &kp RIGHT_ARROW  &kp ENTER  &none  &none  &none  &none  &none  &none  &none  &kp BACKSPACE  &kp DELETE  &kp LG(LS(K))  &kp LG(SLASH)  &kp ESCAPE  &tog_on 1  &trans  &trans  &trans  &trans  &tog_on 1
            >;
        };

        raise_layer {
            display-name = "NUMBER";

            // -----------------------------------------------------------------------------------------
            // |       | F10 | F9 | F8 | F7 |  || = | 7 | 8 | 9 | #/$ | TAB   |
            // | CMD W | F11 | F6 | F5 | F4 |  || - | 4 | 5 | 6 | 0   | ENTER |
            // | CMD Q | F12 | F3 | F2 | F1 |  || % | 1 | 2 | 3 | //* | BSPC  |
            //                        |  |  |  ||  |  |  |

            bindings = <
&trans  &kp F10  &kp F9  &kp F8  &kp F7  &none  &kp EQUAL  &kp NUMBER_7  &kp NUMBER_8  &kp NUMBER_9  &mt DOLLAR HASH  &kp TAB  &kp LG(W)  &mt LEFT_CONTROL F11  &mt LEFT_ALT F6  &mt LEFT_SHIFT F5  &mt LEFT_GUI F4  &none  &kp MINUS  &kp NUMBER_4  &kp NUMBER_5  &kp NUMBER_6  &kp NUMBER_0  &kp ENTER  &kp LG(Q)  &kp F12  &kp F3  &kp F2  &kp F1  &none  &kp PERCENT  &kp NUMBER_1  &mt COMMA NUMBER_2  &mt PERIOD NUMBER_3  &mt ASTERISK SLASH  &kp BACKSPACE  &tog_on 2  &trans  &trans  &trans  &trans  &tog_on 2
            >;
        };

        extra_layer_1 {
            display-name = "SYMBOL";

            // -----------------------------------------------------------------------------------------
            // |  | ! | @ | # | $ | % || ^ | & | * | ? | ' |  |
            // |  | + | = | ( | ) | " || : | ] | [ | } | { |  |
            // |  | | | < | - | > | \ || ` | _ | / | ~ | ; |  |
            //               |  |  |  ||  |  |

            bindings = <
&none  &kp EXCLAMATION  &kp AT_SIGN  &kp HASH  &kp DOLLAR  &kp PERCENT  &kp CARET  &kp AMPERSAND  &kp ASTERISK  &kp QUESTION  &kp SQT  &none  &none  &mt LEFT_CONTROL PLUS  &mt LEFT_ALT EQUAL  &mt LEFT_SHIFT LEFT_PARENTHESIS  &mt LEFT_GUI RIGHT_PARENTHESIS  &kp DOUBLE_QUOTES  &kp COLON  &mt LEFT_GUI RIGHT_BRACKET  &mt LEFT_SHIFT LEFT_BRACKET  &mt LEFT_ALT RIGHT_BRACE  &mt LEFT_CONTROL LEFT_BRACE  &none  &trans  &kp PIPE  &kp LESS_THAN  &kp MINUS  &kp GREATER_THAN  &kp BACKSLASH  &kp GRAVE  &kp UNDERSCORE  &kp SLASH  &kp TILDE  &kp SEMICOLON  &none  &tog_on 3  &none  &none  &none  &none  &tog_on 3
            >;
        };

        extra_layer_2 {
            display-name = "MEDIA";

            // -----------------------------------------------------------------------------------------
            // | BT_CLR    | BT_0 | BT_1 | BT_2   | BT_3   | BT_4 ||  |  |  |  |  |  |
            // | SYS_RESET |      | PRED | NEXT   | PLAY   | RGB  ||  |  |  |  |  |  |
            // | BOOTLDR   |      | MUTE | VOL_DN | VOL_UP |      ||  |  |  |  |  |  |
            //                                           |  |  |  ||  |  |

            bindings = <
&bt BT_CLR  &bt BT_SEL 0  &bt BT_SEL 1  &bt BT_SEL 2  &bt BT_SEL 3  &bt BT_SEL 4  &none  &none  &none  &none  &none  &sys_reset  &sys_reset  &none  &kp C_PREV  &kp C_NEXT  &kp C_PLAY_PAUSE  &rgb_ug RGB_TOG  &none  &none  &none  &none  &none  &bootloader  &bootloader  &none  &kp C_MUTE  &kp C_VOL_DN  &kp C_VOL_UP  &studio_unlock  &none  &none  &none  &none  &none  &none  &tog_on 4  &none  &none  &none  &none  &tog_on 4
            >;
        };
    };
};
